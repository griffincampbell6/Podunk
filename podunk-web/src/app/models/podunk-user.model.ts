export interface PodunkUser {
    CREATED_TS: string;
    DISPLAY_NAME: string;
    DOB: Map<string, number>;
    FIRST_NAME: string;
    LAST_NAME: string;
    LOCATION: Map<string, string>
    PRONOUNS: string;
    UPDATED_TS: string;
    USER_UID: string;
}