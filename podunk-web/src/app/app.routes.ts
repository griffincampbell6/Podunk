import { Routes } from '@angular/router';
import { PodunkAccountFormComponent } from './user-registration/account-form/account-form.component';

export const routes: Routes = [
    { path: 'podunk-account-form', component: PodunkAccountFormComponent }
];
